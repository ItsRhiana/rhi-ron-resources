---
import StarlightPage from "@astrojs/starlight/components/StarlightPage.astro";

import "../../styles/fonts.css";
import "../../styles/colors.css";
import "../../styles/style-page.css";

import rainmaker from "../../data/styles/rainmaker-world-cleansing-rain";

import StyleHeader from "../../components/StyleHeader.astro";
import StyleKit from "../../components/StyleKit.astro";
---

<StarlightPage
    frontmatter={{
        title: `${rainmaker.characterName} — ${rainmaker.styleName}`,
        description: `${rainmaker.characterName} — ${rainmaker.styleName} Style details.`,
        tableOfContents: false,
        prev: false,
        next: false,

        head: [
            {
                tag: "style",
                content: `
                    :root {
                        --sl-content-width: 100% !important;
                    }

                    .sl-markdown-content {
                        max-width: none !important;
                    }
                `,
            },
        ],
    }}
>
    <div class="style-page">

        <StyleHeader style={rainmaker} />

        <nav class="style-tabs">

            <button class="active" type="button">
                <span class="tab-label">
                    KIT
                </span>
            </button>

            <button type="button">
                <span class="tab-label">
                    BUILD &amp; SYNERGIES
                </span>
            </button>

            <button type="button">
                <span class="tab-label">
                    REVIEW
                </span>
            </button>

        </nav>

        <StyleKit style={rainmaker} />

    </div>
</StarlightPage>